# market.py — retained for timezone utility only.
# Bot-specific constants (SYMBOLS, PAPER), market_open_state,
# and compute_health have been removed. Bot is on hold.
from zoneinfo import ZoneInfo

TIMEZONE = ZoneInfo("America/New_York")
